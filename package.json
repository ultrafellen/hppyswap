{
  "name": "hppyswap-monorepo",
  "private": true,
  "packageManager": "pnpm@11.13.0",
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
